#Find area of a rectangle
length = float(input("Enter the length of the rectangle: "))
width = float(input("Enter the width of the rectangle: "))

area = length * width

print(f"Lenght of the Rectangle is: {area}cm²") # to get cm² make sure numlock id on then press and hold the alt key and type 0178 on the numpad.